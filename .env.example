# Talos Environment Variables
# Copy this file to .env and customize for your environment.
#
# Environment variables take precedence over config.toml values.
# All variables are optional - sensible defaults are used if not set.

# =============================================================================
# Server Configuration
# =============================================================================
# TALOS_SERVER_HOST=127.0.0.1
# TALOS_SERVER_PORT=8080
# TALOS_HEARTBEAT_INTERVAL=60

# Legacy: Direct server URL override (takes precedence over HOST/PORT)
# TALOS_SERVER_URL=http://127.0.0.1:8080

# =============================================================================
# Database Configuration
# =============================================================================
# Database type: "sqlite" or "postgres"
# TALOS_DATABASE_TYPE=sqlite

# Database connection URL (auto-detects type from prefix)
# SQLite:
# TALOS_DATABASE_URL=sqlite://talos.db
# PostgreSQL:
# TALOS_DATABASE_URL=postgres://username:password@localhost/talos

# =============================================================================
# License Configuration
# =============================================================================
# TALOS_LICENSE_KEY_PREFIX=LIC

# =============================================================================
# Logging Configuration
# =============================================================================
# TALOS_LOGGING_ENABLED=true
# TALOS_LOG_LEVEL=info
