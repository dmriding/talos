# Talos Environment Variables
# Copy this file to .env and customize for your environment.
#
# Environment variables take precedence over config.toml values.
# All variables are optional - sensible defaults are used if not set.

# =============================================================================
# Server Configuration
# =============================================================================
# TALOS_SERVER_HOST=127.0.0.1
# TALOS_SERVER_PORT=8080
# TALOS_HEARTBEAT_INTERVAL=60

# Legacy: Direct server URL override (takes precedence over HOST/PORT)
# TALOS_SERVER_URL=http://127.0.0.1:8080

# =============================================================================
# Database Configuration
# =============================================================================
# Database type: "sqlite" or "postgres"
# TALOS_DATABASE_TYPE=sqlite

# Database connection URL (auto-detects type from prefix)
# SQLite:
# TALOS_DATABASE_URL=sqlite://talos.db
# PostgreSQL:
# TALOS_DATABASE_URL=postgres://username:password@localhost:5432/talos

# =============================================================================
# License Configuration
# =============================================================================
# TALOS_LICENSE_KEY_PREFIX=LIC

# =============================================================================
# Logging Configuration
# =============================================================================
# TALOS_LOGGING_ENABLED=true
# TALOS_LOG_LEVEL=info

# =============================================================================
# JWT Authentication (requires "jwt-auth" feature)
# =============================================================================
# Enable JWT authentication for admin API endpoints
# TALOS_AUTH_ENABLED=false

# JWT secret key for signing/validating tokens
# IMPORTANT: Use a strong, random secret in production (minimum 32 characters)
# Generate with: openssl rand -base64 32
# TALOS_JWT_SECRET=your-super-secret-key-change-in-production

# JWT issuer claim (iss)
# TALOS_JWT_ISSUER=talos

# JWT audience claim (aud)
# TALOS_JWT_AUDIENCE=talos-api

# Token expiration time in seconds (default: 3600 = 1 hour)
# TALOS_TOKEN_EXPIRATION_SECS=3600

# =============================================================================
# Production Example
# =============================================================================
# Uncomment and modify for production deployment:
#
# TALOS_SERVER_HOST=0.0.0.0
# TALOS_SERVER_PORT=8080
# TALOS_DATABASE_URL=postgres://talos:secretpassword@db:5432/talos
# TALOS_AUTH_ENABLED=true
# TALOS_JWT_SECRET=generate-a-strong-secret-here
# TALOS_LOG_LEVEL=info
# TALOS_LOGGING_ENABLED=true
